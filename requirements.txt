# Runtime dependencies for the Vercel deploy.
# @vercel/python uses this file with priority over pyproject.toml — keep it
# minimal: every extra package inflates the lambda bundle and the cold start.
# Heavy libs listed in pyproject.toml (matplotlib, seaborn, reportlab, celery,
# twilio, …) are only referenced by unused modules and must NOT be added here.
Flask==3.1.1
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Werkzeug==3.1.3
SQLAlchemy==2.0.41
psycopg2-binary==2.9.10
requests==2.32.3
dnspython==2.7.0
cryptography==42.0.8
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
